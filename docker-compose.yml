version: '3'
services:
  spring-api:
    image: emptyvessel/spring-api:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    networks:
      - my-shared-network

networks:
  my-shared-network:
    external: true
